name: Move Issue to Development

on:
  create:

jobs:
  update-project:
    if: github.ref_type == 'branch'
    runs-on: ubuntu-latest
    steps:
      - name: Extract issue number from branch name
        id: extract
        run: |
          BRANCH_NAME="${{ github.ref_name }}"
          echo "🌿 Branch criada: $BRANCH_NAME"
          
          # Tenta extrair número da issue (formatos: issue-123, 123-fix-bug, feature/123, etc)
          ISSUE_NUM=$(echo "$BRANCH_NAME" | grep -oE '[0-9]+' | head -1)
          
          if [ -n "$ISSUE_NUM" ]; then
            echo "issue_number=$ISSUE_NUM" >> $GITHUB_OUTPUT
            echo "✅ Issue encontrada: #$ISSUE_NUM"
          else
            echo "⚠️ Nenhum número de issue encontrado no nome da branch"
            echo "issue_number=" >> $GITHUB_OUTPUT
          fi
      
      - name: Update Project Status
        if: steps.extract.outputs.issue_number != ''
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.DEVSTATUS_TOKEN }}
          script: |
            const projectId = "PVT_kwDODixzhM4BFb_a";
            const statusFieldId = "PVTSSF_lADODixzhM4BFb_azg2xh6E";
            const STATUS_EM_DESENVOLVIMENTO = "823324b0";
            
            const issueNumber = ${{ steps.extract.outputs.issue_number }};
            const branchName = context.ref.replace('refs/heads/', '');
            const branchRepo = context.repo.owner + '/' + context.repo.repo;
            
            // IMPORTANTE: A issue está no repositório DevOps
            const issueOwner = 'Ambiente-de-Testes';
            const issueRepo = 'Repo-Test-DevOps';
            
            console.log(`🌿 Branch criada: ${branchName}`);
            console.log(`📂 Repositório: ${branchRepo}`);
            console.log(`📋 Buscando issue #${issueNumber} em ${issueOwner}/${issueRepo}...`);

            try {
              // 1. Busca a issue no repositório DevOps
              const { data: issue } = await github.rest.issues.get({
                owner: issueOwner,
                repo: issueRepo,
                issue_number: issueNumber
              });
              console.log(`✅ Issue encontrada: "${issue.title}"`);
              console.log(`   URL: ${issue.html_url}`);
              console.log(`   Node ID: ${issue.node_id}`);
              const issueNodeId = issue.node_id;

              // 2. Busca o projeto e seus itens
              console.log(`📊 Buscando itens do projeto...`);
              const projectQuery = `
                query ($projectId: ID!) {
                  node(id: $projectId) {
                    ... on ProjectV2 {
                      title
                      items(first: 100) {
                        pageInfo {
                          hasNextPage
                          endCursor
                        }
                        nodes {
                          id
                          content {
                            ... on Issue { 
                              id 
                              number 
                              title
                              repository {
                                nameWithOwner
                              }
                            }
                          }
                        }
                      }
                    }
                  }
                }
              `;
              
              const projectData = await github.graphql(projectQuery, { projectId });
              const items = projectData.node.items.nodes;
              console.log(`📋 Projeto: ${projectData.node.title}`);
              console.log(`📋 Total de itens: ${items.length}`);

              // 3. Encontra o item correspondente
              const projectItem = items.find(i => i.content?.id === issueNodeId);

              if (!projectItem) {
                console.log(`⚠️ Issue #${issueNumber} não está no projeto`);
                console.log(`   Adicione a issue ao projeto primeiro: ${issue.html_url}`);
                return;
              }
              console.log(`✅ Item encontrado no projeto`);

              // 4. Atualiza o status
              console.log(`🔄 Atualizando status para "Em Desenvolvimento"...`);
              const updateMutation = `
                mutation ($projectId: ID!, $itemId: ID!, $fieldId: ID!, $value: String!) {
                  updateProjectV2ItemFieldValue(input: {
                    projectId: $projectId
                    itemId: $itemId
                    fieldId: $fieldId
                    value: { singleSelectOptionId: $value }
                  }) {
                    projectV2Item { 
                      id 
                    }
                  }
                }
              `;
              
              await github.graphql(updateMutation, {
                projectId,
                itemId: projectItem.id,
                fieldId: statusFieldId,
                value: STATUS_EM_DESENVOLVIMENTO
              });
              
              console.log(`🎉 Sucesso!`);
              console.log(`   Issue #${issueNumber} → "Em Desenvolvimento"`);
              console.log(`   Trigger: Branch "${branchName}" criada em ${branchRepo}`);
              
              // 5. Opcional: Comentar na issue
              await github.rest.issues.createComment({
                owner: issueOwner,
                repo: issueRepo,
                issue_number: issueNumber,
                body: `🌿 Branch \`${branchName}\` criada em ${branchRepo}\n\n✅ Status atualizado para **Em Desenvolvimento**`
              });
              console.log(`💬 Comentário adicionado na issue`);
              
            } catch (error) {
              console.error(`❌ Erro: ${error.message}`);
              if (error.errors) {
                console.error('Detalhes:', JSON.stringify(error.errors, null, 2));
              }
              if (error.status === 404) {
                console.error(`Issue #${issueNumber} não encontrada em ${issueOwner}/${issueRepo}`);
              }
              throw error;
            }
